services:
  - type: static
    name: vgatk-bd
    publishDir: dist
    buildCommand: echo "Сборка уже выполнена локально, пропускаем..."
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
    routes:
      - type: rewrite
        source: /*
        destination: /index.html